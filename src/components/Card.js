import React, {
  Component
} from 'react';
import HomePage from './HomePage.js'

class Card extends Component {
  render() {
    return ( <
      >
      <
      HomePage / >
      <
      />
    );
  }
}

export default Card;
